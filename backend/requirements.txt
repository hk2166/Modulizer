# ── API server ────────────────────────────────────────────────
fastapi==0.136.1
uvicorn==0.46.0
pydantic==2.13.4
python-multipart==0.0.28

# ── TTS / inference ───────────────────────────────────────────
TTS==0.22.0
# torch + torchaudio MUST be version-matched.
# Install from the correct PyTorch index for your hardware:
#   CPU:        pip install torch==2.5.1+cpu torchaudio==2.5.1+cpu --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12.1:  pip install torch==2.5.1+cu121 torchaudio==2.5.1+cu121 --index-url https://download.pytorch.org/whl/cu121
#   CUDA 12.4:  pip install torch==2.5.1+cu124 torchaudio==2.5.1+cu124 --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
torchaudio==2.5.1
# transformers>=4.40 removed BeamSearchScorer which XTTS v2 depends on
transformers==4.39.3

# ── Audio processing ──────────────────────────────────────────
librosa==0.11.0
soundfile==0.13.1
numpy==1.26.4
scipy==1.17.1
pydub==0.25.1
ffmpeg-python==0.2.0

# ── Speech-to-text (planned) ──────────────────────────────────
faster-whisper==1.2.1

# ── Database (planned) ────────────────────────────────────────
SQLAlchemy==2.0.49

# ── System monitoring ─────────────────────────────────────────
psutil==7.2.2
